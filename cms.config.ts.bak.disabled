import { defineConfig } from '@vercel/cms';

export default defineConfig({
  contentDir: 'cms/content',
  schemas: {
    // Ana sayfa için içerik şeması
    homepage: {
      title: 'Ana Sayfa',
      fields: {
        title: { type: 'string', required: true },
        description: { type: 'string', ui: { component: 'textarea' } },
        heroImage: { type: 'string', required: true, ui: { component: 'image' } },
      },
    },
    // Aktiviteler için içerik şeması
    activity: {
      title: 'Aktiviteler',
      fields: {
        title: { type: 'string', required: true },
        slug: { type: 'string', required: true },
        description: { type: 'string', ui: { component: 'textarea' } },
        content: { type: 'string', ui: { component: 'markdown' } },
        coverImage: { type: 'string', required: true, ui: { component: 'image' } },
        gallery: { type: 'array', of: { type: 'string', ui: { component: 'image' } } },
        duration: { type: 'string' },
        difficultyLevel: { 
          type: 'string', 
          options: ['Kolay', 'Orta', 'Zor', 'Çok Zor'],
          ui: { component: 'select' }
        },
        includedServices: { type: 'array', of: { type: 'string' } },
        featured: { type: 'boolean', default: false },
        contactWhatsapp: { type: 'string', description: 'WhatsApp iletişim numarası (örn: +905001234567)' },
      },
    },
    // Hakkımızda sayfası için içerik şeması
    about: {
      title: 'Hakkımızda',
      fields: {
        title: { type: 'string', required: true },
        content: { type: 'string', ui: { component: 'markdown' } },
        teamMembers: {
          type: 'array',
          of: {
            type: 'object',
            fields: {
              name: { type: 'string', required: true },
              role: { type: 'string' },
              bio: { type: 'string', ui: { component: 'textarea' } },
              image: { type: 'string', ui: { component: 'image' } },
            },
          },
        },
      },
    },
    // İletişim sayfası için içerik şeması
    contact: {
      title: 'İletişim',
      fields: {
        title: { type: 'string', required: true },
        address: { type: 'string', ui: { component: 'textarea' } },
        email: { type: 'string' },
        phone: { type: 'string' },
        mapLocation: { type: 'string' },
      },
    },
  },
});